name: onlinejudge-api
services:
  postgres-db:
    image: postgres:18.4
    container_name: postgres_db
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "${DB_PORT}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}" ]
      interval: 5s
      timeout: 5s
      retries: 5

  python-init:
    image: docker:cli
    container_name: python_init
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    command: docker pull python:3.10-alpine

  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: onlinejudge_api
    image: duckien185/onlinejudge_api:v1.0
    user: root
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./sandbox:/app/sandbox
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - HOST_SANDBOX_DIR=${PWD}/sandbox
      - ConnectionStrings__PostgreSQLConnection=Host=${DB_HOST};Port=5432;Database=${POSTGRES_DB};Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}
    depends_on:
      postgres-db:
        condition: service_healthy

volumes:
  postgres_data:
